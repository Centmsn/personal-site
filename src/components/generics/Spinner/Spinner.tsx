import { SpinnerProps } from "./constants";
import * as P from "./parts";

const Spinner = ({ label }: SpinnerProps) => {
  return <P.SpinnerWrapper>spinner</P.SpinnerWrapper>;
};

export default Spinner;
